## install & run

    git clone <this-repo>
    pip3 install -r requirements.txt
    # get a mongodb server
    # configure `config.yml`
    # start mitm server
    ./mitm-server.py
    # show stored images and more
    ./manager/app.py
